{
  "name": "flac-bindings",
  "version": "1.2.0",
  "description": "libflac bindings to node.js with easy to use API",
  "repository": "https://github.com/melchor629/node-flac-bindings/",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild": "prebuild -t 4.7.3 -t 6.9.5 -t 7.5.0 --strip"
  },
  "keywords": [
    "flac",
    "encoder",
    "decoder",
    "binding"
  ],
  "author": "melchor9000",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.4.0",
    "prebuild-install": "^2.0.0"
  },
  "devDependencies": {
    "prebuild": "^6.0.0"
  },
  "gypfile": true
}
