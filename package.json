{
  "name": "flac-bindings",
  "version": "1.2.3",
  "description": "libflac bindings to node.js with easy to use API",
  "repository": "https://github.com/melchor629/node-flac-bindings/",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild": "prebuild -t 4.7.3 -t 6.9.5 -t 8.7.0 --strip"
  },
  "keywords": [
    "flac",
    "encoder",
    "decoder",
    "binding"
  ],
  "author": "melchor9000",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.3.0",
    "nan": "^2.10.0",
    "prebuild-install": "^2.3.0"
  },
  "devDependencies": {
    "jshint": "^2.9.5",
    "prebuild": "^6.2.2"
  },
  "gypfile": true
}
