import bindings from 'bindings'

export const {
  _testAsync,
  napiVersion,
  EncoderBuilder,
  Encoder,
  Decoder,
  format,
  metadata,
  metadata0,
  SimpleIterator,
  Chain,
  Iterator,
  fns,
} = bindings('flac-bindings')
